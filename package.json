{
  "name": "pixlab",
  "version": "0.1.0",
  "description": "High-performance WebAssembly image editor",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:dev:wasm\" \"npm:dev:web\"",
    "dev:wasm": "cd rust-wasm && cargo watch -i pkg -s 'wasm-pack build --target web --dev'",
    "dev:web": "cd web && npm run dev",
    "build": "npm run build:wasm && npm run build:web",
    "build:vercel": "npm run build:web",
    "build:wasm": "cd rust-wasm && wasm-pack build --target web --release",
    "build:web": "cd web && npm run build",
    "test": "npm run test:rust && npm run test:web",
    "test:rust": "cd rust-wasm && cargo test",
    "test:web": "cd web && npm run type-check",
    "install:all": "npm install && cd web && npm install",
    "clean": "rm -rf rust-wasm/target rust-wasm/pkg web/dist web/node_modules node_modules"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
